# Core dependencies for Pelican static site generation
pelican>=4.8.0
markdown>=3.4.0
Jinja2>=3.1.0

# Dependencies for Cloudflare integration
requests>=2.28.0
python-dotenv>=0.19.0

# Password hashing for admin authentication
bcrypt>=4.0.0

# JSON handling (already included in Python 3.7+)
# json - built-in

# Optional: For better development experience
pyyaml>=6.0

# Legacy Firebase dependencies (can be removed after migration)
# google-cloud-firestore>=2.7.0
# google-cloud-storage>=2.5.0
# firebase-admin>=6.0.0

# Trello integration (deprecated, can be removed)
# requests-oauthlib
# python-trello
